[package]
name = "sdk_core"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "sdk_core"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.36.1"
eppo_core = { version = "=9.0.0", path = "../../../eppo_core", features = ["rustler"] }
serde_json = "1.0.138"
